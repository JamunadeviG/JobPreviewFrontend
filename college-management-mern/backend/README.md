# College Management Backend (Node/Express/Mongo)

## Setup

```bash
cd backend
npm install
cp .env.example .env   # then edit credentials
npm run dev
```

*Requires MongoDB running locally on default port 27017.*